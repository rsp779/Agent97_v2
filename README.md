# Synthetic Banking Ecosystem Generator

Generates linked JSON datasets for customers, accounts, cards, loans, merchants, transactions, devices, sessions, behavioral signals, fraud records, workflows, and knowledge base content.

Run:

```bash
.venv/bin/python -m banking_generator --small
```

Outputs are written under `output/<preset>/`.

